apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: locplace
  namespace: locplace
  labels:
    app: locplace
spec:
  selector:
    matchLabels:
      app: locplace
  endpoints:
    - port: metrics
      interval: 30s
      path: /metrics

