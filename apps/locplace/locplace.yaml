apiVersion: apps/v1
kind: Deployment
metadata:
  name: locplace
  namespace: locplace
  labels:
    app: locplace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: locplace
  template:
    metadata:
      name: locplace
      labels:
        app: locplace
    spec:
      containers:
        - name: locplace
          image: ghcr.io/locplace/server:0.3.6
          imagePullPolicy: Always
          env:
            - name: MAX_PENDING_BATCHES
              value: "1000"
            - name: DB_MAX_CONNS
              value: "50"
            - name: DATABASE_URL
              value: "postgresql://app:nMBKpeBsq8m6cciK22OvAYb13IlYMiZWya6Babntxwel1pOVqBPIBmRoPGu9cPx0@192.168.1.16:5432/app"
            - name: ADMIN_API_KEY
              valueFrom:
                secretKeyRef:
                  name: locplace-admin-key
                  key: key
          ports:
            - containerPort: 8080
              name: http
            - containerPort: 9090
              name: metrics
      restartPolicy: Always
      
