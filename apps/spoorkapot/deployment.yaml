apiVersion: apps/v1
kind: Deployment
metadata:
  name: spoorkapot
  namespace: applications
  labels:
    app: spoorkapot
spec:
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: spoorkapot
  replicas: 0
  template:
    metadata:
      labels:
        app: spoorkapot
    spec:
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: spoorkapot-data
      containers:
        - name: spoorkapot
          image: ghcr.io/bo0tzz/spoorkapot:main
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          envFrom:
          - secretRef:
              name: spoorkapot-config
          volumeMounts:
            - name: data
              mountPath: /data
