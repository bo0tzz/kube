apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: goldilocks
  namespace: goldilocks
spec:
  interval: 1h
  chart:
    spec:
      chart: goldilocks
      version: '8.0.0'
      sourceRef:
        kind: HelmRepository
        name: fairwinds
        namespace: goldilocks
      interval: 1h
  values:
    dashboard:
      replicaCount: 1