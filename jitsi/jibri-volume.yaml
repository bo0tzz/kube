kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: jibri-recordings
  namespace: jitsi
  annotations:
    volume.beta.kubernetes.io/storage-class: "nfs-storage"
spec:
  storageClassName: nfs-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi